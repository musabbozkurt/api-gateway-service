version: '3'
services:
  rabbitmq:
    image: rabbitmq:3-management
    restart: always
    container_name: test-rabbitmq
    ports:
      - 5672:5672 # default ports
      - 15672:15672 # default ports
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest