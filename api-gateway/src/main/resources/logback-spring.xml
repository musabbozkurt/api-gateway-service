<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Silence Logback internal status messages -->
    <statusListener class="ch.qos.logback.core.status.NopStatusListener"/>

    <!-- Spring Boot defaults -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- Spring properties -->
    <springProperty scope="context" name="service" source="spring.application.name"/>
    <springProperty scope="context" name="logFilePath" source="logging.file:logs/console-${service}.log"/>
    <springProperty scope="context" name="elasticsearchUrl" source="elasticsearch.url"
                    defaultValue="http://localhost:9200"/>

    <property scope="context" name="host" value="${HOSTNAME}"/>

    <!-- Console pattern (human-readable) -->
    <property name="localConsoleLogPattern"
              value="%green(%d{dd-MM-yyyy HH:mm:ss.SSS}) %magenta([%thread]) %highlight(%-5level) [%X{traceId:-},%X{spanId:-},%X{username:-},%X{client_id:-},%X{sessionId:-},%X{api:-}] %logger{36} %msg%n"/>

    <!-- ===== OpenTelemetry Appender ===== -->
    <appender name="OTEL" class="io.opentelemetry.instrumentation.logback.appender.v1_0.OpenTelemetryAppender">
        <captureExperimentalAttributes>true</captureExperimentalAttributes>
        <captureMdcAttributes>*</captureMdcAttributes>
    </appender>

    <!-- ===== Human-readable console (local) ===== -->
    <appender name="LocalConsoleAppender" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${localConsoleLogPattern}</pattern>
        </encoder>
    </appender>

    <!-- ===== JSON console (dev/prod/stage) ===== -->
    <appender name="ConsoleAppender" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <fieldNames>
                <timestamp>timestamp</timestamp>
                <version>[ignore]</version>
                <levelValue>[ignore]</levelValue>
            </fieldNames>
            <includeContext>true</includeContext>
            <throwableConverter class="net.logstash.logback.stacktrace.ShortenedThrowableConverter">
                <maxDepthPerThrowable>100</maxDepthPerThrowable>
                <rootCauseFirst>true</rootCauseFirst>
            </throwableConverter>
        </encoder>
    </appender>

    <appender name="LocalUserActivityElasticAppender" class="com.mb.apigateway.logging.ElasticsearchHttpAppender">
        <indexUrl>${elasticsearchUrl}/logstash-api-gateway-service</indexUrl>
    </appender>

    <!-- ===== Default / local profile ===== -->
    <springProfile name="! (dev | prod | stage)">
        <logger name="USER_ACTIVITY" level="INFO" additivity="false">
            <appender-ref ref="LocalConsoleAppender"/>
        </logger>
        <root level="INFO">
            <appender-ref ref="LocalConsoleAppender"/>
            <appender-ref ref="OTEL"/>
            <appender-ref ref="LocalUserActivityElasticAppender"/>
        </root>
    </springProfile>

    <!-- ===== Dev / Prod / Stage ===== -->
    <springProfile name="(dev | prod | stage)">
        <logger name="USER_ACTIVITY" level="INFO" additivity="false">
            <appender-ref ref="ConsoleAppender"/>
        </logger>
        <root level="INFO">
            <appender-ref ref="ConsoleAppender"/>
            <appender-ref ref="OTEL"/>
        </root>
    </springProfile>
</configuration>
